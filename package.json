
{
  "scripts": {
    
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
   
  },

  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "mongoose": "^8.0.2",
    "save-dev": "^0.0.1-security",
    "vitest": "^1.2.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
